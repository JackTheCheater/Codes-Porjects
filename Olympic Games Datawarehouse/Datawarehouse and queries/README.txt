STEP 5: DATA WAREHOUSE DEFINITION AND EXECUTED QUERIES

The script dw_creation.sql is used to define all of the tables of our DW.
The script queries.sql contains all of the queries we executed on the DW, the first 5 queries are the one discussed in the report used for in-depht analysis while the other 8 queries are the one we used to make relevant outcomes